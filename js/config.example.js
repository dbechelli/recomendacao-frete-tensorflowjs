// Copie este arquivo para js/config.js (gitignorado) e preencha com sua chave.
// Crie a chave no Google Cloud Console com a Distance Matrix API habilitada,
// restrita por referrer HTTP a http://localhost:3000/* (e ao seu domínio de
// apresentação, se houver). Sem isso a cotação por CEP não funciona.
window.GOOGLE_MAPS_API_KEY = '';
